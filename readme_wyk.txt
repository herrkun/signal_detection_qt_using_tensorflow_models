2019.01.09  用的是 debug1文件夹下的工程，目前已经加上了一个线程

2019.01.16 除了模型加载线程，又添加了两个线程，其中一个就是动态显示数字（本来想显示模型预测epoch的进度，但是在另一个线程中调用Python方法貌似和模型调用线程有冲突，会死机

还没有想好解决办法），目前脚本基本上已经完善，可以进行前面板参数的选择了，参数可以加载到脚本中了，并且点击loadrun按钮可以进行预测了，并且预测的结果也都可以保存在本地了，基本功

能已经实现。

2019.01.16 通过一个本地TXT文档的读写，可以实现动态显示预测的进度了

2019.01.17  显示进度的已经加上了进度条

接下来要做的： 图像的显示，多开几个线程，一起进行图像的显示，两种方式：1、数据预测全部完成后，然后返回到QT，进行多线程的绘制，这里要涉及到一个互斥量（显示index）的操作

2、在QT里面进行一批一批的预测处理，也就是说把 for step in batchnum 移动到QT中，然后出来的每一批数据都进行多现成的显示，这样预测和显示就是同步的，但是此种方式较为复杂，程序

之间的逻辑需要里的很清楚。
